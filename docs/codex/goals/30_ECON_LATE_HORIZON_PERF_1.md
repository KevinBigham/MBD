# ECON-LATE-HORIZON-PERF-1 — Exact Season-30 Runtime Prerequisite

Current status: `ACTIVE — late-checkpoint evidence gate`.

## Objective

Unblock Goal 28 after Goal 29's verified optimization still missed the fixed
40-minute sufficiency gate. Authenticate the exact seed-7111 season-29 state,
profile season 30 from identical fresh imports, optimize one newly measured
late-horizon hot path without semantic drift, and admit one unchanged Goal-18
30-primary plus checkpoint-15 replay run only after a conservative forecast
retains 15% headroom.

This is a second bounded prerequisite for roadmap item 18. It does not amend
Goal 29's historical blocker, begin item 19, or implement the general item-98
performance-budget framework.

## Oracle authority

On 2026-07-16 Kevin explicitly authorized continuing with any necessary work.
The coordinator applies that authority narrowly to the new bounded performance
split required by Goal 29 and Goal 18. This permits one disposable late-state
evidence composition, one production writer with at most two correction loops,
one final Sol review, local commits, and local fast-forward landing.

It does not weaken the existing `2_400_000ms` Goal-18 gate, change a receipt or
calibration band, begin item 19, or authorize remote push, deployment, tag,
publication, or release.

## Live-source contract

- Local `main` starts at `cd5e9191118aee76d22d66b7ffed32fed748cae8`.
- Goal 18 remains unlanded at
  `6ce96ebf6482cd0a3fa52a342d7b29e8977528ff`.
- GameSnapshot remains v35 and Dexie remains v6. No migration is authorized.
- Worker state remains canonical. Profiler and checkpoint metadata remain
  test-only and never enter state, snapshots, public Comlink actions, annual
  rows, receipts, deterministic digests, or RNG.
- Goal 29 remains honestly blocked after its consumed two-loop contract. Its
  landed optimization is a prerequisite, not completion evidence.
- The failed full run log at
  `/tmp/econ18-seed7111-full-41b12d1.log` is source handoff evidence only. It
  contains 30 primary starts, 29 completions, no replay, and no receipt.

## Required behavior

1. Reuse the retained authenticated season-15 artifact only after validating
   its raw SHA-256, envelope, producer identity, v35 snapshot, deterministic
   state, raw RNG, row 15, carryover context, and import/re-export identity.
2. In a disposable composition, run canonical Goal-18 seasons 16 through 29
   exactly once and write one unique season-29 artifact outside Git. Stop before
   season 30.
3. Bind the season-29 artifact to the input artifact hashes, exact Goal-18 and
   disposable adapter revision/tree, seed 7111, diagnostic mode, v35, ordered
   rows 16–29, row-29 and subdomain digests, full snapshot, raw RNG,
   `remainingIds`, baseline roster categories, and continuation state.
4. Reject source/tree, seed, mode, schema, season, state, RNG, row, continuation
   context, missing/extra-key, truncated, and recomputed-envelope forgeries
   before installing singleton worker state.
5. From identical fresh season-29 imports, run one warm-up and three measured
   season-30 baselines using Goal 29's existing profiler plus an out-of-band V8
   CPU sample. Publish stage shares, call signature, state sizes, total runtime,
   and comparison with season 16.
6. Sol freezes one dominant actionable production function, exact file, tests,
   and performance bands after the late baseline. Production edits are
   forbidden before that freeze.
7. Only operation-local indexing, guard-first eligibility, provably redundant
   recomputation removal, or an order-preserving equivalent algorithm is
   allowed. No cross-save cache, skipped work, sampling, pruning, parallel sim,
   reordered iteration, changed RNG call, or mechanics suppression.
8. One Terra writer owns the frozen function and at most two correction loops.
   Architecture invalidation returns to Sol. A third loop is forbidden.
9. Every baseline/candidate comparison preserves row 30 byte-for-byte, complete
   deterministic v35 state, raw RNG, contracts, payroll, population, free
   agency, lifecycle, roster, player, team, history, news, checkpoint-30, public
   worker surface, and deterministic content digests.
10. A deliberate disposable mutant must fail the structural negative control
    when the optimized lookup/work fence is restored or bypassed. Restore the
    correct implementation before any gate or commit.
11. Run one candidate warm-up and three serial B/C pairs from identical
    season-29 bytes. Every pair must improve both targeted work and total time;
    ranges must not overlap; stage/call signatures and semantic digests must be
    exact.
12. After source freeze, run one primary-only readiness forecast through season
    30 and exact checkpoint-15 continuation. The forecast is:

    `primary seasons 1–30 + primary seasons 16–30 + fixed replay/import/validation overhead`

    It must be `<= 2_040_000ms`, retaining 15% headroom below Goal 18's fixed
    ceiling. It is performance admission only and never substitutes for the
    final in-process receipt.
13. Focused tests, affected and root typechecks, full tests, production
    build/PWA, route/lazy-shell and unchanged bundle budgets, determinism,
    `git diff --check`, and final Sol review with zero actionable P0–P2 findings
    must pass before landing.
14. Land Goal 30 independently on local `main`, merge it into the Goal-18 WIP
    branch without rewriting Goal-18 history, then run exactly one unchanged
    seed-7111 diagnostic under the external 2,400-second alarm.
15. A failed final run gets no retry, alternate command, timeout grace, new
    seed, calibration interpretation, or item-19 continuation. Record the
    blocker and stop.

## Frozen admission bands

Before the production edit, the exact late profile must show the selected
function/linear-lookup stack consumes at least 25% of
`regularSeason.total`. For the conditionally identified milestone seam:

- median `checkMilestones` CPU time improves by at least 90%;
- median combined `regularSeason.injuryNews` plus
  `regularSeason.tickerDebutConsequences` improves by at least 25%;
- median `regularSeason.total` improves by at least 15%;
- all three B/C pairs improve, and baseline/candidate ranges do not overlap.

If the 25% attribution gate is not met, Sol must re-plan from the measured
season-30 profile. These bands may not be silently lowered.

## Scope cut line

Permanent production scope is empty until Sol's post-profile freeze. The
conditional candidate is only
`packages/sim-core/src/narrative/newsFeed.ts::checkMilestones` plus its adjacent
`packages/sim-core/tests/narrative.test.ts` proof. Any replacement path must be
named in the architecture gate before Terra writes and remain one narrow
existing module.

Disposable composition may modify only Goal-18 test support and one dedicated
test-only late-checkpoint adapter. Those files and every raw checkpoint,
profile, CPU sample, and forecast artifact remain unlanded.

No schema/Dexie/public API/UI/route/dependency/gameplay/RNG/history-retention/
receipt/validator/band/horizon/checkpoint/replay/timeout change; no automatic
pruning; no broad worker decomposition; no item 19; no push, deploy, tag,
publication, or release.

## Proof

- Hostile adapter tests and singleton-empty failure proof.
- Authenticated unique season-29 artifact and exact season-30 baseline receipt.
- V8 CPU attribution plus existing stable stage profiler.
- Structural negative control that fails the pre-optimization work shape.
- Three exact paired season-30 profiles and continuous/resumed row-30 identity.
- Exact horizon/readiness forecast within `2_040_000ms`.
- Focused, full, build/PWA, bundle, typecheck, determinism, and final-review
  receipts.
- Exactly one unchanged Goal-18 seed-7111 receipt under `2_400_000ms`.

## Done

Goal 30 is done only when the measured narrow late-horizon optimization is
semantically exact, all gates and final review are green, the local prerequisite
lands independently, and the unchanged Goal-18 seed-7111 diagnostic emits and
validates its exact 30-primary plus 15-replay receipt within 40 minutes.
