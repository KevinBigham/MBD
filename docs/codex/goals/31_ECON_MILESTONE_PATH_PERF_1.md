# ECON-MILESTONE-PATH-PERF-1 — Exact Combined Milestone Production Path

Current status: `BLOCKED — exact no-retry final admission failed the frozen 2,040,000ms aggregate forecast cap on 2026-07-30; source candidate not landed; recursive proof lineages remain permanently retired`.

## 2026-07-29 exact composition authorization

Kevin explicitly authorized the exact tracked, disposable, non-landed 12-path
composition requested in `DIRECT_PROOF_AUTHORITY.md`, then confirmed full
permission for every bounded action needed to continue. This resolves the
2026-07-28 oracle block without changing the landable production scope.

The source branch may land only the two milestone/ceremony consumers, their two
adjacent tests, Goal/run/status documentation, and ordinary completion
evidence. A separate composition branch may overlay the exact autonomous helper
blob and the eleven named repository-owned proof paths. That branch must derive
its own revision, tree, clean state, and complete path hashes; it must be
independently reviewed before artifact execution and mechanically proven not to
be an ancestor of local `main`. Its helper, harness, adapter, reducer, and proof
bytes may never enter the Goal-31 source commit or landing.

R41, copied recovery machinery, retries, weakened contracts, Item 19, and
remote/release actions remain prohibited.

## 2026-07-27 direct-proof authorization

Kevin authorized a fresh bounded `ECON-MILESTONE-PATH-PERF-1` prerequisite
from current `main`, using only the semantics-neutral milestone/ceremony
optimization and repository-owned direct tests. He also authorized the exact
oracle recorded in `VERIFICATION_STOP_LOSS.md`.

That authorization opened current-main reconciliation, but live-source
preflight found that the exact Goal-18 harness calls
`skipOffseasonPhaseWithAI(state, 'autonomous_league')` while current `main`
exposes only the one-argument interactive implementation. The missing
`OffseasonAutomationMode` production seam is a 269-line change to
`sim.worker.helpers.ts`, with its own adjacent test. Importing it would violate
this goal's explicit two-consumer/no-third-production-module boundary and
Kevin's narrower “using only the milestone/ceremony optimization” instruction.
Changing the harness to interactive mode would violate the preserved Goal-18
gameplay, RNG, and receipt contracts.

Further read-only closure inspection proved that no existing authenticated tree
contains both the season-15 capture seam and the late-range/profile seams, and
no repository-owned real `.cpuprofile` or final admission lane exists.
Therefore no candidate transfer, checkpoint recapture, performance command, or
diagnostic is currently legal. The narrowest resume decision is to authorize
the exact tracked, disposable, non-landed 12-path composition frozen in
`DIRECT_PROOF_AUTHORITY.md`. It overlays only the exact autonomous helper in
production and composes the reviewed repository-owned proof seams. Inside that
composition, exact authenticated season-15 and season-29 bytes may be restored
from a real backup; otherwise they must be recaptured once. None of its helper,
harness, adapter, or proof bytes may enter the four-file Goal-31 commit or
local-main landing.

A separate landed prerequisite or explicit Goal-31 third-module amendment is
also possible but broader. None may be inferred from the current “using only”
instruction. The current authorization does not permit retrying a failed
command or receipt, weakening a band or digest, creating R41 or another
recovery lineage, beginning Item 19, or pushing, deploying, tagging,
publishing, or releasing.

The source-freeze commit and its exact tree/hashes must exist before any raw
checkpoint or performance receipt is admitted.

## 2026-07-27 verification stop-loss

The Gate-M v1/v2/v3 and R37–R40 recovery lineages are historical
non-acceptance evidence. They may not be patched, retried, executed, or copied
forward as R41. Their repeated failures were verification-program and
architecture-contract defects; no production, economy, save, RNG, or gameplay
defect was established.

The active route is the smaller repository-owned boundary frozen in
`docs/codex/runs/ECON-MILESTONE-PATH-PERF-1/VERIFICATION_STOP_LOSS.md`.
The four production/test candidate files remain byte-preserved in the
historical WIP. Transfer onto the fresh current-main branch is paused until the
new source dependency receives explicit authority. Item 19, push, deployment,
tag, publication, and release remain closed.

## Objective

Unblock the item-18 forecast admission path without weakening it by making the
two independently measured late-season career-milestone consumers avoid
full-population display-name work while preserving every observable simulation
fact exactly.

This is a new bounded prerequisite after Goals 29 and 30. It supersedes neither
their history nor their evidence. It neither retries Goal 30's failed forecast
nor begins item 19.

## Authority and scope

- Historical implementation base is
  `main@ace5068f0f49a1195c2937461fe8ad7f04d8d3d8`. Current campaign main is
  `a9343763b35818fd1111ffec3bc3440a8294e6aa`; the dedicated Goal-31 branch is
  `9ba8b35a2e908a484cb5fa21781f91d415777a3a`. The four candidate paths are
  unchanged across that main advance, but current-main reconciliation remains
  mandatory before source freeze.
- GameSnapshot remains v35 and Dexie remains v6; no schema or migration is
  authorized.
- Only these production paths may change:
  `apps/web/src/workers/sim.worker.milestones.ts`,
  `apps/web/src/workers/sim.worker.ceremony.ts`, and their adjacent tests.
- Goal/run/status docs and a separately committed disposable proof runtime are
  allowed. Raw checkpoints, profiles, and timing receipts stay outside Git.
- No third production module, public API, UI, dependency, game mechanic, RNG,
  ordering, history, Goal-18 validator/band/receipt/checkpoint/horizon, timeout,
  pruning, premature local-main landing, remote operation, or item-19 work is
  authorized. Local-main landing is permitted only after every frozen gate is
  green.

## Required behavior

For each consumer separately, build cumulative stats as it does today and:

1. Call `checkMilestones(cumulativeStats, [], season, day)` as the eligibility
   probe.
2. If it emits no player ID, return the consumer's existing empty result before
   reading any player ID.
3. Use qualifying IDs for membership only, filter `state.players` in original
   array order while retaining duplicates, then call `checkMilestones` again
   with that narrowed original-order array.
4. Preserve the clean-main mapping/queue behavior, including first-match and
   missing-player behavior, user-team filtering, event/prose/ID/property order,
   timestamps, and ceremony dedupe.

No cache, handoff, new state, skipped mechanic, reordered iteration, RNG draw,
or approximation is permitted.

## Proof and admission

Adjacent tests prove zero player-ID reads when ineligible; `<= 2N + 4` reads
for 128 rows with only the final qualifying ID; duplicate-first semantics;
missing-player suppression; exact multi-moment JSON; and exact ceremony
properties and user-team filtering. A full-width final-call mutant must fail in
each consumer and be restored before any receipt or commit.

After exact source/hash/provenance validation, run one new candidate warm-up,
three fresh standard candidates C1–C3 ordinally paired with sealed B1–B3, and
three fresh V8 candidates from identical season-29 bytes. Every exact semantic
digest must match. Standard and V8 bands, including the combined-root median
band, are frozen in the architecture gate. Run one new forecast-primary and
only its one continuation when primary permits it. The combined adjusted wall
must be `<= 2,040,000ms`; failure stops this goal without retry or reinterpretation.

## Done

Docs-first and source-freeze commits exist separately; the four allowed source/
test paths use the frozen algorithm; focused semantic/structural, candidate,
V8, bundle/build, and forecast receipts are green; the worktree is clean; and
the candidate is ready for the prescribed Sol review. Full repository gates and
landing are deliberately later-phase work.
