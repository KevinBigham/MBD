# Plan — ECON-MILESTONE-PATH-PERF-1

Status: `ACTIVE — architecture freeze green; exact four-file source transfer ready`.

## Objective and player outcome

The exact long-save simulation reaches career-milestone production work with
less redundant name-resolution cost while players receive the same events and
ceremonies. See [Goal 31](../../goals/31_ECON_MILESTONE_PATH_PERF_1.md).

## Current source truth

The prior source-truth, blocker, and plan records are retained historical
evidence, not current execution authority. The live WIP is
`codex/econ-milestone-path-perf-1` at
`9ba8b35a2e908a484cb5fa21781f91d415777a3a` with an empty index and exactly
four non-document candidate paths: `sim.worker.milestones.ts`
(`bef65c1e23a3fc8096ae8745528cd577e00f93cbc1f4b3909c95d8c86b09bbd6`),
`sim.worker.ceremony.ts`
(`ee6fe50bdc5bb83107b01822840684d71197abc4bed79e7e6ae8f01eb82f0102`), and
their adjacent tests (`6834b7b5915ddb135ed1d28467794922e37980b7ca7a9214263390053ec62123`,
`00bfabd45662a837e7c01d4c3c2581287b39ab2f91719b1f4d25e85fbab66811`).
Current campaign main and origin/main are
`a9343763b35818fd1111ffec3bc3440a8294e6aa`; the four candidate paths did not
change after the branch merge-base.

Historical disposable proof runtime identity:
`/Users/kevin/Downloads/MBD-econ-milestone-path-perf-1-proof`,
`codex/econ-milestone-path-perf-1-proof`, revision
`9ae8722c23e1f4b428faff7caba954b0aa0a2802`, tree
`7b4f5f177b7f40a9d5bc76f0c28d6fb4874ee8cc`. It and every later custom
Gate-M proof checkout are retired, not current execution targets. Package
manager is pnpm; current commands come from `package.json`. GameSnapshot v35
and Dexie v6 remain confirmed and unchanged.

## Scope and non-goals

The landable source branch owns exactly
`sim.worker.milestones.ts`, `sim.worker.ceremony.ts`, their two adjacent tests,
and bounded Goal/run/status/changelog/completion documentation. Those four
source/test bytes may be temporarily mutated only for the two mandatory
negative controls and must be restored byte-exact before receipts.

A separate tracked, disposable, non-landed composition branch derived from the
immutable source freeze may overlay only the exact `sim.worker.helpers.ts` blob
and the eleven repository-owned proof paths listed in
`DIRECT_PROOF_AUTHORITY.md`. It may combine capture/range support, derive
source/composition identity, validate a real `.cpuprofile`, and perform final
ordinal/band/sidecar/forecast admission. No package/config/balance/profiler/
action/snapshot/trade-finance/sim-core/gameplay/schema/UI/dependency path may
change. Raw artifacts use fresh `/tmp` paths and never enter Git.

The composition commit may never become an ancestor of local `main`; its helper
and proof bytes may never enter the source branch or landing. R41, custom
recovery machinery, retries, altered gameplay/RNG/save/history/receipt
contracts, weakened bands/timeouts, Item 19, push, deployment, publication,
tag, and release remain outside scope.

Before composition freeze, its repository-owned direct tests must prove:
generated full-closure identity; `capture|validate` season-15 ownership;
explicit run class and ordinal; in-isolate `node:inspector` V8 capture; rejection
of unknown/cyclic CPU graphs; complete sidecar/profile/receipt reopening; exact
semantic comparison; all standard/V8/forecast bands; and an exact-key `wx`
admission receipt. These are proof-code tasks inside the authorized 12 paths,
not production corrections.

Baseline facts are a composite immutable authority, not one missing manifest:
Goal-30 `SOURCE_TRUTH.md` blob `3d162280…` owns B1–B3, row/state/RNG/call, and
build V8; late-reference CPU-test blob `a17c250c…` owns ceremony/combined/
sim-to-playoffs V8 roots; Goal-31 blocker blob `c1a5cb9c…` owns the exact
semantic digest. The admission reducer rehashes every constituent and rejects
any fact drift.

The composition owns only pre-artifact focused/typecheck/review and the
capture/performance/admission matrix. After matrix admission, root typecheck,
full tests, determinism, build/PWA, and bundle gates run on the landable source
branch with current-main helper bytes and composition-only proof paths absent.
Final Sol reviews that exact landable diff plus the admitted receipts and
confirms the composition is not its ancestor. Only the source branch may be
staged or fast-forwarded to local `main`.

## Behavioral invariants

The worker remains canonical; state, raw RNG, save/export/round-trip, history,
rows, subdomain/content digests, player/team/roster facts, and public surface
remain exact. Original player order and duplicates remain exact. CPU and user
rules are unchanged. No save mutation or browser interaction is touched.

## Design decision

Each consumer performs an empty-player canonical eligibility probe, returns
early when none qualify, derives membership IDs, retains qualifying original
rows in original order, then makes its final canonical call against that array.
This removes irrelevant display-name scans without encoding threshold policy or
sharing work across consumers. A separate full-width mutant confirms the bound.
No migration is needed; rollback is reverting the eventual source-freeze commit.

## Historical Gate-M v2 recovery decision

> Retired by `VERIFICATION_STOP_LOSS.md`. The facts below explain the stopped
> lineage but grant no current implementation or execution authority.

V1 attempt 8 is immutable consumed evidence, not a retry candidate. Its PRE
process-isolation rejection occurred before compiler/VM work, but the wrapper
threw before preserving the rejected inventory. The existing ancestor-only
rule also conflicts with the authorized Vitest host's legitimate Vite/esbuild
descendants.

`SOL_RECOVERY_ARCHITECTURE.md` freezes a new Goal-31
`item18-adapter-materialization-v2/attempt-1` lineage. It admits only the exact
authority cone around the authenticated Node process—its launcher ancestors,
self, and transitive descendants—and continues to reject siblings, cousins,
and unrelated same-checkout processes. Rejections must preserve the original
`ps` bytes and exact offending rows before throwing. The quarantined v1
checkout may not be modified.

## Historical milestones 1–18

> These entries record what occurred before the stop-loss. Every forward
> Gate-M/Gate-N/v2/v3/source-freeze instruction below is retired and must not be
> dispatched. The only current forward graph is in the 2026-07-27 stop-loss
> resumption section.

1. Docs-first: create Goal/run source truth, plan, and architecture gate; commit
   separately. **Status: in progress.**
2. Milestone consumer: implement frozen probe/narrow/final-call algorithm and
   add exact structural/semantic tests. Prove with its focused Vitest file.
3. Ceremony consumer: implement independently and add queue exactness tests.
   Prove with its focused Vitest file.
4. Run both negative controls, restore source, then focused typecheck/build and
   bundle evidence.
5. Extend and test the disposable adapter/profile only; profile schema becomes
   3 and forecast schema becomes 2 for fresh receipts, freezing candidate and
   canonical `newsFeed.ts` hashes with existing Goal-18 identities unchanged.
6. Run exact focused proof, the two WIP negative controls (restore exact bytes
   after each), focused WIP tests, typecheck, production build, and bundle
   budgets. Record 0/2 new disposable correction loops unless a permitted
   proof/command correction is actually needed.
7. Commit exactly the three disposable proof files, verify tracked-clean state,
   then execute strict serial `validate-output → warm-up → C1 → C2 → C3 →
   V8-1 → V8-2 → V8-3 → forecast-primary → conditional continuation`.
8. Update this plan with literal commands, revisions, output hashes, timings,
   and admission result. Stop before root full gates, Sol review, WIP commit,
   landing, Goal 18, or item 19.
9. Create a separate disposable v2 proof worktree from the exact v1 candidate
   snapshot. One Terra writer may change only process-policy/runtime/test/config
   under the frozen architecture.
10. Run focused topology/failure-state tests, the required negative control,
    and web typecheck; then freeze source and obtain independent mechanical and
    Sol zero-P0–P2 static verdicts.
11. Create and independently verify a new execution authority, create its
    anchor last, and run exactly one v2 attempt with retries disabled.
12. Continue to Gate N and the fresh performance matrix only after the v2
    success receipt and verifier are reopened and accepted.
13. Quarantine the failed pre-freeze v2 candidate without executing it. Its
    exact final review found an infeasible authority hash cycle, an unsealed
    compiler-only terminal state, under-validated failure evidence, incomplete
    recursive attempt-8 binding, and incomplete malformed-root/AST controls.
14. Start a fresh v3/attempt-1 proof lineage from the exact archived v2 patch.
    Freeze the acyclic `R -> P -> F -> M/S -> A -> V -> H -> RUN -> T`
    evidence graph before Terra edits. V3 is a new bounded strategy, not v2
    correction loop 4.
15. Implement only the four proof files, run the clean-environment contract
    probe and focused gates, then freeze exact source and review it twice.
16. Create v3 evidence strictly in dependency order and run exactly one dynamic
    attempt with retries disabled. Stop permanently on any red/partial result.
17. The first clean v3 contract probe proved that the runtime is a Tinypool
    worker, not the outer Vitest config process. Preserve R and supersede only
    its launch topology with immutable R2. Require two byte-identical dual-launch
    probes before P/F; this amendment consumes no Gate-M attempt or source loop.
18. The R2 foundation proved 25/25 focused tests and dual-launch capture, then
    stopped because R/R2 did not freeze exact evidence JSON schemas. Preserve
    the foundation and author immutable R3 with the complete ordered schema and
    terminal ledger. This architecture-only amendment consumes no correction
    loop. Terra must replace every active inherited v2 authentic-execution path
    before source review.

## Historical acceptance matrix

> This matrix records the pre-stop-loss route. Rows naming Gate M, Gate N,
> v2/v3 authority, or recovery execution are `RETIRED — NON-ACCEPTANCE
> EVIDENCE`, regardless of their historical status text.

| Requirement | Location | Proof | Status |
| --- | --- | --- | --- |
| Probe and order-preserving narrowing | two worker consumers | adjacent structural tests | pending |
| Exact event/moment behavior | two worker consumers | controlled JSON/state/RNG fixtures | pending |
| Full-width mutants fail | temporary disposable edits | two failing focused tests then restored | pending |
| No schema/API/bundle regression | unchanged boundaries | typecheck/build/bundle | pending |
| Late-state semantic and timing bands | disposable runtime | fresh C1–C3/V8 receipts | pending |
| Forecast cap | disposable runtime | one new primary, conditional continuation | pending |
| V1 attempt-8 quarantine | `ATTEMPT8_QUARANTINE.json` | exact embedded bytes/hash plus consumed counts | complete |
| V2 authority-cone process isolation | disposable four-file maximum | hostile topology matrix; PRE/POST/compound failure receipts | architecture frozen; writer pending |
| V2 Gate-M one-shot | fresh v2 namespace | fresh source freeze, two static reviews, authority/verifier/anchor, one command | pending |
| V2 pre-freeze quarantine | `GATE_M_V2_QUARANTINE.json` | exact four hashes, green focused receipt, final P1/P2 verdict, zero dynamic attempts | complete |
| V3 acyclic evidence chain | v3 namespace | architecture, prewriter snapshot, source freeze, two reviews, A/V/H order | architecture ready |
| V3 complete terminal settlement | four-file proof boundary | PRE/compiler-only/POST/compound/success exact receipts and terminal reopen | pending |

## Progress log

1. Verified exact clean base, scripts, v35, Dexie v6, source hashes, and live
   consumer seams. Committed the docs-first artifact `3f4bf71` before production.
2. Completed two-consumer implementation, adjacent 6/6 focused proof, restored
   negative controls, web typecheck, and Vite/PWA build.
3. Correction 1/2 invalidated overlapping standard processes before a receipt;
   correction 2/2 invalidated one fail-closed C2 environment parse. The
   corrected serial warm-up, C1, and C2 receipts are exact. C3 repeated the
   malformed producer tree and is the forbidden third correction. Stop.
4. Resume authorization `mbd-item18-goal31-resume-20260718-cap2` supersedes
   only the old evidence-admission route. It preserves the preceding history,
   starts a new disposable-proof correction ledger at 0/2, requires wholly
   fresh serial standard/V8/forecast receipts, and forbids any production
   source correction. Preflight on 2026-07-18 confirmed the specified WIP and
   proof revisions/hashes, no live named adapter process, and an absent fresh
   output namespace. Next: WIP focused baseline, then disposable-only adapter
   and CPU profile implementation.
5. WIP focused baseline passed 6/6. Disposable runtime commit
   `da74b976f68bc2be4287e1af9d5c24b9002db433` (tree
   `6e1ecdc35b41a4d1c07200a417ab04b2e6440d8e`) contains only the authorized
   adapter and CPU extractor/test. `vitest run econMilestonePathCpuProfile.test.ts
   econLateHorizonPerf.integration.test.ts` passed 9 with 1 intentional skip;
   web typecheck passed. The extractor reproduced sealed CPU roots exactly:
   build 48,951,209us, ceremony 24,549,831us, combined 73,501,040us, and
   simToPlayoffs 127,114,740us. New disposable correction ledger: 1/2 (the
   first focused run exposed incomplete schema-3/forecast-2 test expectations;
   no production bytes changed).
6. Pre-performance WIP gates passed: both mandatory full-width mutants failed
   their structural bounds at 8,385 reads versus `<=260`, were restored with
   all four protected SHA-256 values exact, focused plus bundle tests passed
   15/15, typecheck passed, and production build passed with worker-core
   454.92kB raw under the unchanged 456,704/147,456 budgets. Fresh
   `validate-output` first rejected a mistyped parent producer tree; the one
   corrected invocation then reached retained-artifact parsing but failed
   `Season-29 artifact keys are missing or extra.` No simulation, standard,
   V8, or forecast receipt was emitted. This is an exact adapter/retained
   receipt compatibility mismatch and is terminal under the frozen resume
   contract; do not make a third disposable correction or launch the matrix.
7. Coordinator adjudication supersedes the provisional terminal reading above.
   Disposable correction ledger is now exhausted at **2/2**: (1) commit
   `22eddc893a08268d7adcbf0a3291346d4edef49c` / tree
   `c90ffa6ac7d319e2818af85373613398c92d2b0b` restored the distinct retained
   Goal-30 season-29 exact-key boundary; (2) commit
   `99469fd916092f05cc17407f82657919c03c69e4` / tree
   `c2cac8bb4c09c4b82b935d7b756162f26d212c63` added the independently omitted
   Goal-31 fresh profile hash keys. Its focused adapter tests and typecheck are
   green. The failed pre-fix warm-up emitted no receipt and is rejected; run a
   fresh absent output/time/log path. No further proof or command correction is
   authorized: any defect, drift, failed band, failed forecast, overlap, or
   ambiguous process outcome is terminal.
8. Later Gate-M v1 work in the quarantined proof checkout reached one authorized
   attempt after fresh loop-3 static admission, authority verification, and
   anchor. Attempt 8 failed at its PRE process observation with
   `ITEM18_PROCESS_UNEXPECTED_RELEVANT`; compiler/VM/success artifacts remained
   zero and no retry occurred. `ATTEMPT8_QUARANTINE.json` preserves the exact
   4,316 bytes. Kevin's subsequent explicit one-bounded-fix direction authorizes
   the new v2 recovery architecture only. The v2 writer has not started.
9. The v2 writer completed three bounded loops and reached 20/20 focused tests,
   typecheck, and diff-check green without running Gate M. Final mechanical and
   Sol review still returned `FIX_AND_REVIEW`: an authority self/mutual hash
   cycle made execution infeasible; compiler-only failure had no terminal
   receipt; failure evidence and recursive quarantine binding were incomplete;
   malformed-root/AST controls remained incomplete. V2 is quarantined with zero
   dynamic attempts. Sol froze a fresh v3 strategy under the same Goal 31.
10. Terra's initial v3 foundation reached 23/23 gate-disabled focused tests but
    its clean launch probe failed closed because runtime argv/execArgv belonged
    to a Tinypool worker. Sol amended the architecture to authenticate both the
    config-time outer launch and live inner worker launch. No Gate-M attempt,
    source freeze, authority, or terminal artifact was created.
11. Under R2 the same foundation reached 25/25, two identical inert probe
    payloads, typecheck, and a non-reserved A→V→H pre-ps seam. Terra then found
    that exact v3 P/F/M/S/A/V/H and terminal schemas were not frozen. Sol issued
    R3 as an exact-schema amendment; no authentic attempt was armed.

## Historical decision log

> Items below preserve decisions made inside the retired route. They are not
> current authority. `VERIFICATION_STOP_LOSS.md` and the 2026-07-27 resumption
> section supersede every forward-looking “must”, “next”, or “current
> authority” statement below.

1. The old Sol route and failed execution history remain evidence; the explicit
   cap2 resume contract is current authority for fresh proof admission.
2. Separate adjacent tests are necessary because clean main has no tests for
   either consumer and the scope prohibits widening a shared module.
3. No C3, V8, forecast, full gate, review, source freeze, or landing may run
   because the frozen correction budget is exhausted.
4. Attempt 8 is not the old C3 correction ledger and is not retryable. V2 uses
   a new lineage and attempt ordinal, regenerates every current source/review/
   authority artifact, and inherits v1 only as quarantined history.
5. Goal 31 remains the correct goal because production scope, algorithm, and
   performance contracts are unchanged. Goal 32 is forbidden unless evidence
   invalidates one of those boundaries.
6. The exhausted v2 candidate may not receive a fourth patch or an execution
   authority. V3 must use an acyclic evidence DAG and one private terminal
   settlement path; any backward hash dependency or unsealed terminal state is
   a pre-execution stop.
7. R2 supersedes only R's single-launch assumption. R remains immutable history;
   all current preparation/source/review/authority evidence must bind R2 and the
   two matching probe receipts.
8. R3 supersedes only R/R2 evidence-schema underspecification. R and R2 remain
   immutable inputs. No authentic v3 authority or terminal artifact may use an
   inherited active v2 schema, path, or settlement function.

## Completion conditions

The v1/v2/v3 and R37–R40 proof machinery is retired and cannot satisfy this
goal. Completion now requires the stop-loss route in
`VERIFICATION_STOP_LOSS.md`: current-main reconciliation, the preserved
four-file production candidate, repository-owned focused/negative/typecheck/
build proof, an immutable production source freeze, one internally complete
fresh C1–C3/V8/forecast matrix under newly explicit authority, full gates,
final Sol review, and landing. Item 19 remains closed.

## 2026-07-27 stop-loss resumption

The earlier milestones and decision log remain immutable history. They do not
grant current execution authority.

Live Goal-31 state is branch `codex/econ-milestone-path-perf-1` at
`9ba8b35a2e908a484cb5fa21781f91d415777a3a`, with an empty index and exactly
four unstaged candidate paths. Current campaign `main` and `origin/main` are
`a9343763b35818fd1111ffec3bc3440a8294e6aa`; neither production seam changed
between the branch merge-base `ace5068f0f49a1195c2937461fe8ad7f04d8d3d8`
and current main.

The recursive verifier is replaced, not repaired. No R41, R40A, private
program generator, copied authority graph, or `.swarm` path is acceptance
bearing. The active proof boundary is:

1. preserve the exact four candidate files;
2. reconcile them onto current main without changing their algorithm;
3. run their two adjacent tests and both full-width negative controls;
4. run affected typecheck, build/PWA, and bundle budgets;
5. commit the immutable four-file source freeze;
6. bind a fresh disposable performance runtime to that source-freeze revision
   and real producer output shapes;
7. run one serial warm-up/C1–C3/V8/forecast sequence only after explicit
   performance-execution authority;
8. stop on any semantic, identity, band, forecast, source, or review failure;
9. run full gates and final review only after the candidate matrix is green.

Current-revision focused proof on 2026-07-27 passed both adjacent files, 6/6,
with retries disabled. No performance, simulation, or publication lane ran.

## 2026-07-27 authorized current-main route — stopped before execution

The sole forward route now begins from clean
`main@a9343763b35818fd1111ffec3bc3440a8294e6aa` in
`/Users/kevin/.codex/worktrees/goal31-direct/MBD-main-main` on branch
`codex/econ-milestone-path-perf-1-direct-proof`. The docs-first reconciliation
currently ends at `278618333d09f513c5295b9d3f8b694fca9df9c7`.

The preserved WIP is evidence/source input only. Its exact four candidate
hashes remain the required transfer target. Current-main baseline hashes are
milestones
`ac7a6fe97118096f81fb650e2ba50d15235a25a7524bc4807d498e6784f7e31b`
and ceremony
`b09aeb2f6566a20d1b4abba11d798fb5936a6dd93a4392d9bcaa49e746a44125`.
Neither seam nor canonical `checkMilestones` changed after merge-base
`ace5068f0f49a1195c2937461fe8ad7f04d8d3d8`.

Pre-execution inspection invalidated step 1 of the direct route. Goal 18's
canonical test support calls the two-argument autonomous-league worker seam,
but current `main` exposes only the one-argument interactive seam. The missing
production dependency is `sim.worker.helpers.ts`; importing it would violate
this plan's explicit no-third-production-module scope. A test-only imitation
would violate Goal 18's exact simulation, RNG, and receipt contracts.

The sequence remains frozen for use only after the exact tracked, disposable,
non-landed 12-path composition in `DIRECT_PROOF_AUTHORITY.md` receives explicit
authority:

1. record explicit authority for the exact 12-path proof composition; complete
   2026-07-29; do not touch raw artifacts yet;
2. reconcile Goal-31 source truth from current main;
3. transplant exactly the two production files and two adjacent tests;
4. run focused proof, both full-width mutants, restoration hashes, affected
   typecheck, bundle, and production build;
5. commit the immutable four-file source freeze;
6. create one disposable tracked proof composition from that commit containing
   only the exact helper overlay and bounded repository-owned proof paths;
   combine capture/range support, generated current-source/composition
   provenance, in-isolate real CPU-profile receipt, explicit run ordinals, and
   final admission lanes; reject unknown/cyclic profile graphs and hostile
   identity/sidecar/receipt inputs; run focused tests/typecheck and obtain a
   zero-P0–P2 review before freezing its revision/tree;
7. select exactly one input route inside that frozen composition: authenticate
   restored exact season-15 and season-29 bytes with complete provenance, or
   generate them once through canonical tests outside Git; admit their exact
   producer/tree and semantics before profiling;
8. run one warm-up, C1–C3, three V8 samples, forecast-primary, and only the
   permitted continuation, serially and without retry;
9. stop on any identity, semantics, RNG, save, receipt, band, timeout, source,
   or process failure;
10. on green only, return to the landable source branch, prove current-main
   helper bytes and absence of composition-only paths, run root typecheck/full
   tests/determinism/build/PWA/bundle gates there, obtain final zero-P0–P2 Sol
   review of the landable diff plus admitted matrix receipts and non-ancestry,
   then write completion docs, land only the source branch, integrate Goal 18,
   and run one seed-7111 diagnostic.

Every process retains the `2,400,000ms` ceiling. Forecast-primary plus its
continuation must remain `<=2,040,000ms` after the frozen external adjustment.
Item 19 and every remote/release action remain closed.

## 2026-07-30 direct-proof execution result — terminal forecast stop

Steps 1–8 above executed in order against source freeze
`4e016cc4fe3043e438cc0cbc3aeec798b6f47d6b` and reviewed composition
`2f3329b0886396cd9d8550aa42ea2738d02c4126`, with retries disabled.
Canonical tests generated and validated fresh season-15 and season-29
checkpoints. Warm-up, C1–C3, and V8-1–V8-3 preserved the frozen semantic,
row-30, state, RNG, round-trip, and call-signature facts. The standard and V8
subgates cleared their frozen bands.

Forecast-primary passed in `1,739.71s`; its permitted continuation passed in
`1,209.16s`. They converged exactly on season-30 state, RNG, round-trip,
snapshot, population, and season facts. Final admission then ran once and
failed at the frozen aggregate forecast cap:

`1,739,710 + 10 + 1,209,160 + 10 = 2,948,890ms`

The result exceeds `2,040,000ms` by `908,890ms`. Per steps 9–10 and the
stop-loss oracle, root full gates, final Sol merge review, completion, source
landing, Goal-18 integration, the seed-7111 diagnostic, Item 19, and every
remote/release action did not run. No retry or reinterpretation is authorized.
