# Terra Gate-M v3 Writer Brief

> **RETIRED HISTORICAL EVIDENCE.** Superseded by the 2026-07-27 verification
> stop-loss. Do not dispatch this brief or execute its recovery lineage.

Historical role:
Terra is the sole implementation writer for the fresh Goal-31 Gate-M
v3/attempt-1 proof lineage.

Historical outcome:
Implement the acyclic evidence, process-isolation, launch-authentication, and
terminal-settlement contracts frozen in `SOL_GATE_M_V3_ARCHITECTURE.md`, then
return a focused-test/typecheck-backed source candidate for independent review.

Inputs:
- v3 worktree `/Users/kevin/Downloads/MBD-item18-gate-m-v3-20260722`;
- v3 prewriter source and recovery-architecture artifacts in its `.swarm` run;
- exact v2 failed-candidate hashes in `GATE_M_V2_QUARANTINE.json`;
- immutable v1 attempt-8 quarantine and failure-8 evidence;
- Goal-31 production WIP hashes recorded by `SOURCE_TRUTH.md`.

Constraints:
- Edit only process policy, runtime, test, and special Vitest config.
- Use `apply_patch`.
- Do not edit scripts, docs, production optimization, sim-core, schemas, saves,
  performance bands, Goal 32, Item 19, or a fifth file.
- Do not create source-freeze/review/authority/anchor evidence.
- Do not run Gate M, compiler/VM graph proof, Gate N, exact-45, or a performance
  matrix.
- Do not stage or commit.
- Preserve v1 and v2 quarantines byte-for-byte.

Acceptance:
- v3 process observation and full hostile/AST matrix pass;
- clean-environment contract probe passes and cannot arm Gate M;
- PRE, compiler-only, POST, compound, success, and permanent-consumption tests
  exercise the actual private terminal seam;
- failure and success verifiers exact-check identities, counters, ordering,
  schemas, and mutual exclusion;
- source contains an acyclic normalized launch contract with A/H sentinels and
  no self/later-hash dependency;
- focused tests pass with retries disabled;
- web typecheck and `git diff --check` pass;
- status is exactly the four permitted files with a clean index;
- no v3 terminal or authority artifact exists.

Return:
Exact four-file hashes/modes/bytes, commands/results, finding disposition,
negative-control receipt, canonical-path absence, and any unproven requirement.
Do not claim Gate-M success or source freeze.
