# Direct-Proof Authority — ECON-MILESTONE-PATH-PERF-1

Date: 2026-07-27

Status: `AUTHORIZED FOR SOURCE RECONCILIATION AND EXACT 12-PATH COMPOSITION`.

## User authorization

Kevin authorized a fresh bounded `ECON-MILESTONE-PATH-PERF-1` prerequisite
from current `main`, using only the semantics-neutral milestone/ceremony
optimization and repository-owned direct tests. He permanently prohibited R41
and custom recovery-lineage machinery, preserved the 40-minute ceiling and
exact gameplay/RNG/save/receipt contracts, authorized one final diagnostic,
and kept Item 19 closed until Item 18 passes. He then explicitly authorized
the exact direct-proof oracle in `VERIFICATION_STOP_LOSS.md`.

On 2026-07-29, immediately after the coordinator requested authorization for
the exact tracked, disposable, non-landed 12-path proof composition, Kevin
authorized continuation “full force” and granted permission to inspect the
repository. He then explicitly confirmed full authorization for whatever
bounded work is needed and directed that all relevant files reflect it. This
authorizes the exact composition, source-bound identity/admission reducer,
authenticated restore or one canonical checkpoint recapture, and mechanical
non-landing gates described here. It does not reopen any action this contract
continues to prohibit.

## 2026-07-28 executable-closure refinement

Read-only inspection proved that no existing authenticated repository tree can
execute the whole direct route:

- Goal-18 support at `6ce96eb` is the canonical harness baseline but contains
  neither the exported season-15 `onCheckpoint` seam nor the late-range APIs.
- Goal-29 support blob `81604882` has the reviewed season-15 `onCheckpoint`
  capture seam but no 15→29 or 29→30 range/profile APIs.
- Goal-31 proof support blob `1495b707` has the reviewed range/profile APIs but
  removed `onCheckpoint`.
- Goal-29's season-15 adapter imports the removed APIs and freezes historical
  producer identities that a fresh current-main composition may not claim.
- the portable CPU helper has only in-memory fixture tests; no repository-owned
  `.cpuprofile` receipt command or final matrix/forecast adjudicator exists.

This is an architecture-contract gap in disposable proof code, not a Goal-31
production defect. The smallest executable route is one tracked, non-landed
composition commit with exactly the 12 paths frozen below. It is not R41 or a
recovery lineage.

### Exact disposable path boundary

Only one production file may differ from the Goal-31 source freeze:

- `apps/web/src/workers/sim.worker.helpers.ts`, exact blob
  `9d304b3a0a4af5f83603697e017df6545795d316`.

The composition may add or edit only these repository-owned proof paths:

- `apps/web/src/workers/sim.worker.autonomousOffseason.test.ts`;
- `apps/web/src/workers/econLongSoak.metrics.ts`;
- `apps/web/src/workers/econLongSoak.metrics.test.ts`;
- `apps/web/src/workers/econLongSoak.receipts.ts`;
- `apps/web/src/workers/econLongSoak.receipts.test.ts`;
- `apps/web/src/workers/econLongSoak.test.ts`;
- `apps/web/src/workers/econLongSoak.testSupport.ts`;
- `apps/web/src/workers/econLongSavePerf.integration.test.ts`;
- `apps/web/src/workers/econLateHorizonPerf.integration.test.ts`;
- `apps/web/src/workers/econMilestonePathCpuProfile.ts`;
- `apps/web/src/workers/econMilestonePathCpuProfile.test.ts`.

The bounded edits may only combine the canonical Goal-18 baseline, reviewed
Goal-29 season-15 capture seam, and late-range/profile seams; replace obsolete
provenance with the actual
source-freeze/composition identities, add a fail-closed real `.cpuprofile`
receipt lane, and add the final ordinal/band/sidecar/forecast admission lane.
No package script, config, balance test, profiler, worker action, snapshot,
trade-finance, sim-core, or other production file may change.

## Authority interpretation and live-source limit

“Fresh from current main” requires the candidate and every receipt to bind the
current-main-derived source freeze. The historical season-15 and season-29
files are absent from every known local path. Exact original bytes could be
restored from a real backup only if their recorded hashes and complete
provenance verify; otherwise current raw checkpoints must be created by
canonical repository tests after source freeze. Inventing them from summaries,
copying `.swarm` evidence, or relabeling old hashes remains forbidden.

Live inspection then proved that current `main` cannot execute the canonical
32-organization harness: Goal-18 test support requires
`skipOffseasonPhaseWithAI(state, 'autonomous_league')`, but current `main`
implements only `skipOffseasonPhaseWithAI(state)`. The required
`OffseasonAutomationMode` change is production behavior in
`sim.worker.helpers.ts`, not a repository-test fixture. The preserved Goal-18
candidate at revision `6ce96ebf6482cd0a3fa52a342d7b29e8977528ff`
explicitly owns that seam and requires `autonomous_league` to apply existing
CPU offseason policy to the selected user organization. Replacing it with the
interactive path would change the exact simulation and receipts.

Because the newest authorization says “using only” the two milestone/ceremony
production consumers and the frozen Goal-31 contract prohibits a third
production module, the broad trailing authorization cannot safely be read as
silently amending this narrower boundary.

The four production/test candidate hashes, algorithm, exact semantic digests,
standard and V8 bands, `2,040,000ms` forecast cap, `2,400,000ms` per-process
ceiling, save v35, Dexie v6, and Goal-18 receipt/horizon remain unchanged.

## Frozen executable sequence

1. Record the 2026-07-29 authorization for one tracked, disposable, non-landed
   composition commit derived from the Goal-31 source freeze and limited to the
   exact 12-path boundary above. Complete.
2. Reconcile Goal 31 from current main and re-freeze source truth.
3. Exact four-file candidate transfer.
4. Focused tests and two deliberate full-width negative controls.
5. Typecheck, bundle budget, and fresh production build.
6. Immutable source-freeze commit.
7. Create and review the disposable tracked 12-path composition from that
   commit; freeze its revision/tree before any artifact action; no private
   programs.
8. Select exactly one input route inside the frozen composition: authenticate
   restored exact season-15 and season-29 bytes with complete provenance, or
   perform one fresh canonical season-15 capture and season-15→29 capture.
9. One warm-up, C1, C2, C3, V8-1, V8-2, V8-3, forecast-primary, and only its
   permitted continuation, each on a fresh output path, serially, no retry.
10. Full gates and independent final Sol review.
11. Goal-31 completion/local landing, Goal-18 integration, and exactly one
    seed-7111 diagnostic.

Steps after item 1 are executable only in order. Any failed command,
missing/partial artifact, identity mismatch, semantic/RNG/
save/history drift, negative-control failure, band miss, forecast miss,
timeout, retry, source mutation after freeze, or actionable final P0–P2 stops
the route. Item 19, R41, custom recovery, push, deployment, tag, publication,
and release remain forbidden.

### Generated identity and non-landing requirements

- The composition must derive its own live `HEAD`, tree, clean-state verdict,
  and exact hashes for the four source-freeze files plus all 12 disposable
  paths. Caller-supplied revision/tree strings are never admission authority.
- Hostile tests must reject dirty state, stale revision/tree, missing/extra
  paths, changed bytes, duplicate/missing ordinals, tampered profiles or timing
  sidecars, and bypassed bands/caps.
- When no authenticated raw backup exists, baseline admission uses this
  immutable composite map and no other source:
  - Goal-30 `SOURCE_TRUTH.md` blob
    `3d162280c911b923889276a9031ffa9765d9137a`, SHA-256
    `25011a0f457774b4a2e6999137b24cfa2cfee84ffefb4412d6076158a5d37061`,
    owns B1–B3 totals/combined values, row/state/RNG/call facts, and the
    `48,951,209µs` build V8 root;
  - preserved late-reference CPU-test blob
    `a17c250c202233071c9e8a451d7c9402c752e3ca`, SHA-256
    `fa55622512058311b9a0d73e7033dcd01104d49b7c50b9a84789655ff4fa0350`,
    owns ceremony `24,549,831µs`, combined `73,501,040µs`, and
    `simToPlayoffs` `127,114,740µs`;
  - Goal-31 blocker blob
    `c1a5cb9cc8daf83163c06c0a2de7763fea3a860b`, SHA-256
    `b34e836a586fa31596518f8c0148faa459621f5743f0339e627f93fe1e958571`,
    owns semantic digest
    `f76ac9d30b5edaecc80f4213d5799431a3025cada9398afeb1a749e832dba98b`.
  The reducer must rehash all three and reject any constituent or fact drift.
  These facts may not be re-created from summaries or rerun.
- The source branch remains at the four-file freeze. The disposable
  composition lives on a separate branch/worktree derived from it. Before
  landing, prove the composition commit is not an ancestor of `main`, the
  helper equals current-main baseline on the source branch, disposable proof
  paths are absent there, and the staged landing scope is exact.

### Mandatory direct-proof closure

- Compose `econLongSoak.testSupport.ts` from the canonical Goal-18 baseline,
  Goal-29 `onCheckpoint` capture seam, and late-range/profile APIs. Preserve
  the exact autonomous 32-organization path.
- Reduce the season-15 adapter to source-bound `capture|validate`; remove its
  obsolete profile mode and every historical producer claim. It must bind the
  actual four-file source freeze, actual 12-path composition, helper, harness,
  profiler, save-v35, row/state/RNG/envelope/round-trip facts, and both adjacent
  Goal-31 test hashes.
- Preserve the six late-horizon execution modes while consuming the new
  season-15 contract. Every execution receipt must digest explicit `runClass`
  and `ordinal`; filenames are not execution identity and a raw receipt may
  not satisfy more than one ordinal.
- Produce the real V8 profile inside an allowed repository-owned test path
  around the exact season-30 operation, using `node:inspector` and exclusive
  `wx` output. An outer `node --cpu-prof vitest` profile is insufficient
  because it does not prove which Tinypool isolate executed the operation.
- Harden the profile reducer to reject unknown sample node IDs, cyclic call
  graphs, duplicate nodes/parents, misaligned arrays, non-finite deltas,
  missing/ambiguous roots, nested overlap, and wrong source suffixes.
- Add one repository-owned final admission lane that reopens and rehashes the
  warm-up, C1–C3, V8-1–V8-3, forecast-primary, permitted continuation, timing
  sidecars, and profiles; recomputes receipt digests; rejects missing, extra,
  duplicate, reordered, or reused ordinals; checks every exact semantic/state/
  RNG/row/subdomain/call fact; applies all frozen standard/V8 bands; adds
  10ms per forecast process; enforces `<=2,040,000ms`; and writes an exact-key
  receipt with `wx`.
- Hostile tests must independently fail stale identity, dirty source, changed
  bytes, duplicate/reused/reordered ordinals, semantic drift, malformed or
  tampered sidecars/profiles, unknown/cyclic CPU graphs, every individual band,
  and the final forecast cap.

### Gate and review ownership

1. Before artifacts, the exact committed composition runs its focused matrix
   and web typecheck and receives an independent zero-P0–P2 Sol review.
2. Only that reviewed composition may run capture, standard, V8, forecast, and
   admission lanes.
3. After a green matrix, root typecheck, full tests, determinism, production
   build/PWA, and bundle gates run on the exact landable source branch or its
   docs-only completion descendant, with current-main helper bytes and all
   composition-only proof paths absent.
4. Final Sol reviews the exact landable diff together with the admitted matrix
   receipts, rehashes the source/composition identities, and mechanically
   confirms the composition commit is not an ancestor of the proposed landing.
5. Exact staging and local-main fast-forward may occur only from the source
   branch. The composition branch is never merged, rebased onto the landing,
   cherry-picked, or used as a completion-doc parent.
