# ECON-FA-DECISIONS-1 Execution Plan

## Objective and player outcome

Ship roadmap item 16 as one causal loop:

`factual offer + age-shaped priorities + projected opportunity + contender facts + loyalty -> deterministic decision -> exact durable signing -> same reason in Press Room after reload`

The loop must be symmetric for user and CPU clubs, bounded against the existing
economy, honest about the absent future-usage promise system, and isolated from
items 17+.

## Live source truth

- Worktree/branch: `/Users/kevin/Downloads/MBD-fa-decisions-16` on
  `codex/fa-decisions-16` at `7af6781d3672bb78c16702bb3321c4fcf0d89a07`.
- Local `main` matches the base; `origin/main` is
  `fd217dc57262cd104f4fc140cb6e6c571cfa9290`.
- Item worktree began clean. Three protected dirty files in the separate main
  checkout remain outside this slice and must remain byte-identical/unstaged.
- Root declares `pnpm@9.15.4`; GameSnapshot is v34 and Dexie is v6.
- Baseline focused sim-core, web worker/UI/exact-save, typecheck, snapshot, and
  migration receipts are green and recorded in `SOURCE_TRUTH.md`.
- Two read-only source/test maps reconstructed the architecture. Parent is sole
  writer. No model-routed relay claim is made.

## Scope and non-goals

- Owned: pure decision types/evaluator, factual worker context, shared user/CPU
  integration, stable ordering, exact reason-bearing signing news, bounded
  existing offer/Press UX, focused tests, 4x4 study, production reload proof,
  docs, review, and local-only landing.
- Deferred: persistent playing-time enforcement, schema/version change,
  historical motive backfill, broad Market Intelligence rewrite, unified org
  identity, difficulty redesign, item 17 retention/cash, item 18 30-season soak,
  new routes/dependencies, push/deploy/tag/release.

## Behavioral invariants

- Worker truth, exact save authority, actual offered/persisted salary equality,
  roster capacity, QO compensation, ordered durability, and truthful `Saved`
  remain hard boundaries.
- The evaluator is pure, bounded to a `12%` market-value nonfinancial ceiling,
  and consumes no RNG. Offer/timing RNG stays seeded and input-order stable.
- No hidden potential, user identity, difficulty, fan sentiment, or GM spending
  preference enters the player decision.
- Old saves preserve facts and historical copy. Only new decisions receive new
  explanations.
- Only item-16 paths may be staged. Protected main-checkout files remain
  byte-identical and unstaged.

## Design decision

Use the exact frozen model in Goal 26. Projected opportunity is derived from
current positional need and is not editable; this prevents a costless fake
promise. Keep structured evaluation transient through the worker action and AI
progress result. Persist the factual explanation by enriching the existing
`NewsItem.body`, which already survives snapshot/export/import and feeds Press
Room. Do not add a save field or reconstruct prior motives.

## Milestones

| # | Checkpoint | Files | Proof command/test | Status |
|---:|---|---|---|---|
| 1 | Reconcile source, tests, goal, exact model, and scope cuts | Goal 26; run `SOURCE_TRUTH.md`/`PLAN.md` | clean preflight; doc consistency review | completed |
| 2 | Implement pure evaluator and deterministic CPU/user selection | sim-core FA module/index/tests | factor matrix, parity, permutations, negative controls, sim-core typecheck | completed |
| 3 | Build factual worker context and atomic reason propagation | bounded worker FA module; helpers/actions/consequences/tests | source twins, salary equality, rejection digest, QO/capacity, CPU/user news | completed |
| 4 | Add honest offer preview and durable result/Press coverage | existing FA components/hooks/routes/tests; Press tests | accessibility/UI tests; exact-save retained snapshot/retry/fence | completed |
| 5 | Freeze source and run calibration/production/root gates | item-16 study; browser spec/evidence | 4x4 study; full suite/build/PWA/determinism/reload/browser | completed |
| 6 | Adversarial review, closeout, scoped commit, local-main FF | run docs, changelog/status/GOAL | MERGE_READY; cached diff check; local landing | in progress — review green; landing pending |

## Acceptance matrix

| Requirement | Implementation | Unit | Integration | Browser/soak | Status |
|---|---|---|---|---|---|
| FAD-1 bounded age curve | pure weights/evaluator | exact boundaries/sums/scores | — | study factor rows | complete |
| FAD-2 factual context | worker context builder | role/standings/tenure/bond twins | query preview + CPU/user use | controlled fixture | complete |
| FAD-3 symmetry | shared evaluator | identity/difficulty/potential controls | literal salary parity | user/CPU reason proof | complete |
| FAD-4 determinism | sorted generation/choice | map/offer permutations; no RNG | same-seed worker digest | 4x4 RNG receipts | complete |
| FAD-5 atomic legality | existing mutation seams + evaluation | invalid/rejection digests | QO/budget/slot/market tuple | reject/accept journey | complete |
| FAD-6 durable reason | enriched canonical signing news | reason copy/dedupe | exact-save retry/fence/reload | Press hard reload | complete |
| FAD-7 compatibility | no schema/backfill | old market defaults | v34/matrix/v33 round-trip | migrated new signing | complete |
| FAD-8 economy bounds | item-16 study | microcases | 4 seeds × 4 seasons | retained item-15 bands | complete |
| FAD-9 accessible UX | existing offer/Press surfaces | component/hooks | production build | desktop + 375x667 | complete |
| FAD-10 repository safety | scoped docs/source | focused/root gates | final review | local-only landing | review complete; landing pending |

## Progress log

- 2026-07-15: Created the clean item-16 branch/worktree from landed item 15.
- 2026-07-15: Verified branch, revisions, remote lag, protected dirty files,
  save/Dexie versions, focused tests, and absence of prior Goal 26/run/browser
  evidence.
- 2026-07-15: Read-only source/test maps confirmed separate user/CPU rules,
  unsupported/dead reason paths, hidden salary drift, order dependence, and the
  lack of a real role-promise authority.
- 2026-07-15: Froze projected-opportunity semantics, factual sources, exact
  weights/floor/tie-break, compatibility rule, study bands, and scope cuts before
  production edits.
- 2026-07-15: Added the shared pure evaluator, factual worker context, literal
  salary parity, deterministic AI ordering, reason-bearing user/CPU news, offer
  preview, exact-save retry proof, and compact-v33 new-signing coverage.
- 2026-07-15: Corrected the study to capture every phase-transition signing.
  Its first complete receipt exposed the post-inflation affordability seam; AI
  competition inflation and forced fallback now recheck the exact same-day
  spending limit and emit a transient acceptance receipt.
- 2026-07-15: Kevin selected `gpt-5-6-swarm` for the remaining run. Swarm
  ledger `fa16-swarm-20260715` is active; the parent remains sole writer and
  source-frozen read-only review lanes will fan out under a peak of two.
- 2026-07-15: Corrected the first swarm findings: preference ranking now uses
  raw contributions; imported canonical market/player identity is rebound only
  when persisted facts are deeply equal; literal AAV, total value, and signing
  bonus stay coherent; and the study independently derives payroll and compares
  complete signing tuples while emitting literal rows.
- 2026-07-15: The authoritative four-seed/four-season split study replayed each
  seed exactly. All 16 season rows stayed inside the frozen item-15 envelopes
  with zero missing reasons, unsupported facts, duplicate decisions, invalid
  roles, over-capacity signings, unaffordable CPU acceptances, or reconciliation
  errors.
- 2026-07-16: Final Sol review found one reproducible P1: a direct 11-year user
  offer could bypass the live 10-year maximum. The evaluator now owns that
  upper bound; pure and worker regressions prove `invalid_contract` plus
  byte-identical snapshot and RNG on rejection.
- 2026-07-16: Corrected current-revision gates passed: focused 41/41 sim-core and
  194/194 worker tests, root typecheck 9/9, full sim-core 1,709 and web 2,434
  passed plus nine intentional skips, contracts 24, UI 1, determinism 3/3,
  production build/PWA, Goal-16 Chromium 1/1, and reload-smoke 2/2. The final
  correction review returned `MERGE_READY` with zero P0–P3.

## Decision log

1. Goal-16 role acceptance is a truthful current-roster projected MLB
   opportunity; item 28 retains future usage enforcement.
2. Player-side age weights sum to the same `12%` nonfinancial ceiling in every
   career stage; money remains dominant.
3. Contender facts are completed standings/playoffs only; loyalty is persisted
   tenure plus matching homegrown bond only.
4. Chemistry/reputation are symmetric; user-only fan, philosophy, difficulty,
   and hidden identity are excluded.
5. The actual offered AAV is both evaluated and persisted.
6. Structured decisions remain transient; existing persisted news body is the
   durable factual artifact. No schema bump or history backfill.
7. Four-by-four is bounded item-16 evidence; item 18 retains 30 seasons.
8. CPU affordability is proven at the acceptance seam. Goal 16 does not turn
   existing advisory user budget guidance into a hard signing prohibition.

## Completion conditions

All FAD rows require observed focused, compatibility, exact-save, study, root,
production browser/reload, and review evidence. Completion also requires a
scoped commit on local `main`, unchanged protected user files, and no push,
deploy, tag, publication, release, or item-17 work. Before commit, roll back only
this slice's owned paths; after commit, revert the single item-16 commit. With no
schema change, rollback requires no migration or historical repair.
