# ECON-EXTENSION-AI-1 — Living Plan

## Objective and ownership

Implement [Goal 23](../../goals/23_ECON_EXTENSION_AI_1.md), the bounded
completion slice for TRUE GOAT roadmap item 13 only. The parent thread is the
sole writer. Three read-only source/test/risk lanes are complete and their
P0-P2 findings are incorporated below.

## Work class and route

- Class: HIGH_RISK / HEAVY because the slice crosses offseason decision RNG,
  player contracts/history, imported aggregate validation, and exact
  persistence.
- Relay availability: persistent model-routed child controls are unavailable.
  Use the user-authorized manual relay-pattern fallback: parent Sol-pattern
  architecture, parent Terra-pattern sole implementation writer, one read-only
  Sol-pattern final review, and parent Luna-pattern mechanical closeout.
- Focused loop: sim-core contracts/front-office; worker extension/phase/snapshot;
  exact-save coordinator; bounded extension study; Offseason/News/History UI;
  affected package typecheck.
- Source freeze: one root typecheck/test/build/determinism pass, then one fresh
  production extension journey and existing reload-smoke.
- Correction budget: at most two implementation correction loops before a new
  bounded split is required for any remaining reproducible P0/P1.

## Checkpoints

### 1. Pure identity policy and lawful economics

- [x] Add persisted current-GM personality to transient extension context.
- [x] Define bounded priority, term, and concession effects without altering
  player willingness/demand/acceptance or real budget.
- [x] Replace old AAV with new AAV and enforce the exact budget ceiling.
- [x] Prove candidate legality, personality divergence, storage-order stability,
  identity-neutral player rules, and no roster/tenure side effects.

Gate: EXT-1/2/3/5 pure tests green.

### 2. Canonical worker orchestration and integrity

- [x] Use exact service days and remove hidden potential from the extension
  identity input.
- [x] Bind every CPU club to a versioned team/season-scoped RNG and news RNG;
  preserve parent RNG and unaffected-team outcomes across user-team swaps.
- [x] Record one accepted/rejected history/phase/news fact and validate imported
  aggregates before mutation without inventing legacy rejection history.
- [x] Revalidate public user-team ownership, legal terms, terminal status, and
  runtime session identity before RNG or mutation.

Gate: EXT-4/6/7/8 worker and compatibility tests green.

### 3. Exact persistence and bounded economy evidence

- [x] Prove an actual extension phase transition rolls back pre-acceptance
  failure or retains only the accepted exact post snapshot for retry/fail-close.
- [x] Run bounded multi-seed evidence for eligible/attempted/accepted/rejected,
  terms, AAV, budgets, duplicates, identity divergence, and replay digest.
- [x] Preserve the existing league accepted-extension band and ensure free-agent
  turnover is not silently consumed.

Gate: EXT-9/10/11 focused persistence, snapshot, and balance tests green.

### 4. Player-facing production proof

- [x] Reuse Offseason ledger, News/Press Room, Player History, and archive
  surfaces; add no route and raise no bundle ceiling.
- [x] Add a fresh-production Playwright journey for public phase entry, seeded
  CPU extension, exact durable receipt, hard reload, exact facts, and no replay.
- [x] Inspect relevant desktop and 375x667 controls for readability, keyboard
  reachability, containment, and non-occlusion.

Gate: EXT-12 UI/browser proof green with one worker, zero retries/flakes.

### 5. Freeze, review, and land

- [x] Observe the identity-neutralization negative control fail, restore it, and
  rerun focused gates.
- [x] Freeze source; run root typecheck, full tests, PWA build, determinism,
  bundle budget, `git diff --check`, and existing reload-smoke once.
- [x] Apply `mbd-review-slice` through one read-only adversarial review. Fix and
  recheck every P0-P2 finding within the correction budget.
- [x] Complete requirement mapping, changelog, roadmap/status receipt, rollback,
  remaining risks, and relay retrospective.
- [x] Stage only item-13 paths, run cached diff checks, commit intentionally,
  and fast-forward local `main`. Do not push/deploy/tag or begin item 14.

Gate: EXT-13 green, `MERGE_READY`, exact staged scope, protected main-checkout
changes untouched and unstaged.

## Live source, scope, and invariants

- Source root/branch/base: `/Users/kevin/Downloads/MBD-cpu-extensions-13`,
  `codex/cpu-extension-ai-13`, `c589f0e49e1e9b99c0b6497616786e20a39ec217`.
- Package manager: `pnpm@9.15.4`. GameSnapshot remains v34; Dexie remains v6.
- Owned production seam: sim-core extension policy/results, canonical worker
  orchestration/integrity, exact-save regression proof, bounded study, and the
  existing Offseason/News surfaces. No new route or dependency is permitted.
- The worker remains canonical and Zustand remains a mirror. One scoped RNG per
  season/team/candidate context owns CPU decisions; no parent RNG consumption or
  bare `Math.random()` is allowed.
- The exact save session must retain the accepted post snapshot without replay;
  rejected/no-op work must not claim persistence.
- Player demand and real budget stay identity-neutral. Current-GM personality
  changes only bounded team priority, term, opening posture, and counter
  aggression.
- Old saves keep their recorded facts and honestly empty histories. Item 49's
  permanent cross-domain identity, item 14, item 18, and manual multi-round user
  session persistence remain deferred.

## Acceptance and progress log

| Checkpoint | Artifact/result | Status |
| --- | --- | --- |
| Architecture | Goal 23, source truth, and three read-only source/test/risk maps froze the CPU-only persisted-GM design | Complete |
| Pure policy | contracts/front-office focused run: 33/33 | Complete |
| Worker/persistence/UI | 12 focused files / 269 tests; balance 9/9; sim-core/web typecheck green | Complete |
| Economy evidence | four seeds, 123 attempts, 122 accepts, 1 reject; zero budget overages, duplicates, history mismatches, or parent-RNG changes; exact replay | Complete |
| Negative control | neutralized GM adjustment failed the intended persisted-identity test; restored test passed 1/1 | Complete |
| Root correction freeze | typecheck 9/9, full tests, build/PWA, determinism 3/3, bundle budget green | Complete |
| Production proof | final authoritative CPU extension 1/1 plus reload-smoke 2/2 in 4.8m; one Chromium worker, zero configured retries/flaky classification | Complete |
| Final review | same read-only Sol-pattern thread closed both correction loops; final `MERGE_READY` 0/0/0 | Complete |
| Landing | exact item-13 staging, intentional commit, and local-main fast-forward | Complete |

The source-freeze study exposed one late budget defect: a negotiated final
concession could exceed the exact owner ceiling even when the opening offer was
legal. The accepted-contract commit now rechecks replacement payroll against
the real budget; the same four-seed study is green after the correction.

The first final review returned `FIX_AND_REVIEW` with 0 P0 / 4 P1 / 1 P2.
The first bounded correction loop restored shared non-extension archetype semantics,
introduced extension-only observable prospect scoring, isolated every CPU
candidate's offer/negotiation RNG lanes, rejected nonterminal imported receipts,
made the public offer predicate total over runtime shapes, added the missing
hostile tests, and corrected premature documentation. The same review thread's
recheck returned `FIX_AND_REVIEW` with 0 P0 / 1 P1 / 0 P2 because the candidate
seed still included GM identity and therefore shifted player-side willingness
draws. The second and final authorized correction removed both GM personality
and team-building archetype from the player-side seed while retaining identity
effects in the team's priority/term/opening/counter policy. A direct regression
now proves equal player targets and first walk-away draws across GM postures.
Focused, root, build/PWA, determinism, and 4.8-minute browser rechecks are green;
the same review thread's final verdict is `MERGE_READY` with zero P0–P2.

## Decision log

1. Existing persisted `gmPersonalities`, not derived competitive window alone,
   is the narrow durable identity axis for item 13.
2. Team-scoped stable RNG replaces observer-dependent sequential parent forks.
3. Exact `serviceTimeDays` and observable current ability own inputs; legacy
   service years and hidden ceiling/potential do not.
4. Imported current-season extension rows form one validated relational
   aggregate. Historical missing rejection history remains honest absence.
5. The public mutation is hardened against stale/forged calls, but the broader
   manual session persistence redesign is not absorbed into this CPU slice.

## Rollback

Before commit, revert only this slice's owned paths. After commit, revert the
single item-13 commit. GameSnapshot remains v34 and Dexie remains v6, so there
is no migration rollback and no historical identity or extension fact is
invented.
