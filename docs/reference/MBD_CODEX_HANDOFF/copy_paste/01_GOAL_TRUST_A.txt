/goal Implement docs/codex/goals/01_TRUST_A.md using $mbd-implement-slice and the source-grounded TRUST-A plan. Work through every checkpoint, keep the progress log current, add tests alongside code, verify real IndexedDB hard-reload behavior and failure recovery, run all targeted and full repository gates, perform adversarial self-review, fix all P0/P1 findings, and write COMPLETION.md. Do not broaden scope. Stop only when the goal's done state is proven or a documented stop condition blocks safe completion.
